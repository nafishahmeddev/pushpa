import Spinner from "@app/components/ui/Spinner";

export default function PendingComponent() {
  return (
    <div className="h-dvh w-full flex items-center justify-center text-center">
      <div>
        <Spinner />
      </div>
    </div>
  );
}
